welcomeUI <- function() {
  fluidPage(
    h2("Welcome"),
    p("Explore species distribution models across regions and time."),
    p("Use the Dashboard tab to interact with raster predictions.")
  )
}