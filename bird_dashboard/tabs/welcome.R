welcomeUI <- function() {
  tabItem(
    tabName = "welcome",
    h2("Welcome"),
    p("This dashboard explores bird distributions across regions and time."),
    p("Use the Dashboard tab to interact with the data.")
  )
}
